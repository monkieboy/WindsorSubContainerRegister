WindsorSubContainerRegister
===========================

Using subcontainer in Windsor to aid testing by wrapping the 'real' Windsor Container and implementing a Container for testing which has the 'real' container as the backing.
